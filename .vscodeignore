.vscode/**
.vscode-test/**
out/test/**
out/**/*.map
src/**
.gitignore
tsconfig.json
vsc-extension-quickstart.md

# Dev / docs (not needed at runtime)
.cursor/**
.issues/**
docs/**
scripts/**
.github/**
eslint.config.mjs
CONTRIBUTING.md
versions/**
*.vsix

# Demo source — do not ship 70+ MB MP4 inside the extension; README links to GitHub raw instead
demo.mp4

# Do NOT ignore node_modules: this extension requires the `typescript` runtime package (see package.json dependencies).
# vsce only bundles production dependencies; devDependencies stay out of the VSIX when you use the default flow.
